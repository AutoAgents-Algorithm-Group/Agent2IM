
services:
  app:
    build: 
      context: ..
      dockerfile: docker/Dockerfile
    ports:
      - "9000:9000"
    restart: unless-stopped
    volumes:
      - ../src/config/config.yaml:/app/config.yaml:ro
